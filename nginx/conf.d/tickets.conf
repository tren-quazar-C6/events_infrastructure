# ============================================================================
# Portal Tickets (taquilla) — tickets.quasar.andrescortes.dev
# Backend: events_tickets (ASP.NET Core)
# ============================================================================

server {
    listen 80;
    server_name tickets.quasar.andrescortes.dev;

    access_log /var/log/nginx/tickets_access.log main;
    error_log  /var/log/nginx/tickets_error.log  warn;

    resolver 127.0.0.11 valid=30s;

    location / {
        set $upstream "http://tickets:8080";
        proxy_pass $upstream;

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
    }
}